rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP demo: open read/write — tighten with Firebase Auth for production
    match /employees/{docId} {
      allow read, write: if true;
    }
    match /attendance_logs/{docId} {
      allow read, write: if true;
    }
  }
}
